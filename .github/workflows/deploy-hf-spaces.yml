name: Deploy to Hugging Face Spaces

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Push to HF Spaces
        run: |
          git remote add hf-spaces https://alexbell1:${{ secrets.HF_TOKEN }}@huggingface.co/spaces/alexbell1/hipocampo-mcp
          git push hf-spaces main --force
